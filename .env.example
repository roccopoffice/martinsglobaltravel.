# Cloudflare Worker secrets — set with: npx wrangler secret put NAME --keep-vars
AUTH_SECRET=generate_a_long_random_string
ADMIN_PORTAL_PASSWORD=your_admin_password
STRIPE_SECRET_KEY=sk_live_...
STRIPE_WEBHOOK_SECRET=whsec_...

# Optional — flight search (Book Travel stays hidden until these are set)
AMADEUS_API_KEY=
AMADEUS_API_SECRET=
AMADEUS_ENV=test
